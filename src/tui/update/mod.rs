//! State transitions driven by input messages.
//!
//! Placeholder: the key and mouse handling still lives inline in the `run`
//! loop of `tui::mod`. These submodules are where it will move.

pub mod keys;
pub mod mouse;
